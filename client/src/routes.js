import Home from '@/components/Home';
import SignUp from '@/components/SignUp';

const routes = [
    {
        path: '/',
        component: Home
    },
    {
        path: '/signup',
        component: SignUp
    }
];

export default routes;